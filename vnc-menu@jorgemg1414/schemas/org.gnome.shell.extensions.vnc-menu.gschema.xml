<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.vnc-menu"
          path="/org/gnome/shell/extensions/vnc-menu/">

    <key name="connections-dir" type="s">
      <default>'~/Documentos/VNC'</default>
      <summary>Carpeta de conexiones</summary>
      <description>
        Carpeta donde se buscan los archivos de conexión (.vnc, .remmina).
        Se admite ~ como atajo de la carpeta personal.
      </description>
    </key>

    <key name="vnc-command" type="s">
      <default>'remmina -c vnc://%h:%p'</default>
      <summary>Comando para conexiones VNC</summary>
      <description>
        Plantilla del comando que se ejecuta para un archivo .vnc.
        Sustituciones: %h = host, %p = puerto, %u = usuario, %n = nombre, %f = ruta del archivo.
        Si el programa no está instalado se usan alternativas automáticas
        (vncviewer, xtigervncviewer).
      </description>
    </key>

    <key name="remmina-command" type="s">
      <default>'remmina -c %f'</default>
      <summary>Comando para archivos .remmina</summary>
      <description>
        Plantilla del comando que se ejecuta para un archivo .remmina.
        Sustituciones: las mismas que en vnc-command.
      </description>
    </key>

    <key name="file-manager-command" type="s">
      <default>'nautilus %f'</default>
      <summary>Comando para abrir la carpeta</summary>
      <description>
        Plantilla del comando de «Abrir carpeta». %f se sustituye por la ruta de la carpeta.
      </description>
    </key>

    <key name="enable-checks" type="b">
      <default>true</default>
      <summary>Comprobar disponibilidad</summary>
      <description>Muestra un punto verde/rojo según si el puerto responde.</description>
    </key>

    <key name="check-interval" type="i">
      <range min="10" max="3600"/>
      <default>60</default>
      <summary>Intervalo con el menú abierto (segundos)</summary>
      <description>
        Cada cuántos segundos se refresca el estado mientras el menú está abierto.
      </description>
    </key>

    <key name="background-check-interval" type="i">
      <range min="0" max="86400"/>
      <default>0</default>
      <summary>Intervalo en segundo plano (segundos)</summary>
      <description>
        Cada cuántos segundos se comprueban los hosts con el menú cerrado.
        0 desactiva las comprobaciones en segundo plano: solo se consulta la red
        cuando abres el menú, que es lo recomendable en portátiles o fuera de la
        red local.
      </description>
    </key>

    <key name="check-timeout" type="i">
      <range min="1" max="30"/>
      <default>2</default>
      <summary>Tiempo de espera de la comprobación (segundos)</summary>
      <description>Segundos que se espera a que el puerto responda antes de marcarlo como caído.</description>
    </key>

    <key name="enable-search" type="b">
      <default>true</default>
      <summary>Buscador en el menú</summary>
      <description>
        Muestra un campo de búsqueda al principio del menú cuando hay muchas
        conexiones, para filtrar escribiendo.
      </description>
    </key>

    <key name="search-threshold" type="i">
      <range min="0" max="500"/>
      <default>8</default>
      <summary>Conexiones a partir de las cuales aparece el buscador</summary>
      <description>
        Número mínimo de conexiones para que se muestre el campo de búsqueda.
      </description>
    </key>

    <key name="show-host" type="b">
      <default>true</default>
      <summary>Mostrar host:puerto</summary>
      <description>Muestra el host y el puerto a la derecha del nombre de cada conexión.</description>
    </key>

    <key name="panel-icon" type="s">
      <default>'computer-symbolic'</default>
      <summary>Icono del panel</summary>
      <description>Nombre del icono simbólico que se muestra en la barra superior.</description>
    </key>

  </schema>
</schemalist>
